{
  "name": "crumbtrail-topology-harness",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('crumbtrail-topology-harness is parked: it depends on session post-processing (postProcess, buildLlmBundle, startFastFinalizer, isHighSeverityEvent) that crumbtrail-node no longer exports. Restore those imports before unparking.')\"",
    "typecheck": "node -e \"console.log('crumbtrail-topology-harness is parked: it depends on session post-processing (postProcess, buildLlmBundle, startFastFinalizer, isHighSeverityEvent) that crumbtrail-node no longer exports. Restore those imports before unparking.')\"",
    "test": "node -e \"console.log('crumbtrail-topology-harness is parked: it depends on session post-processing (postProcess, buildLlmBundle, startFastFinalizer, isHighSeverityEvent) that crumbtrail-node no longer exports. Restore those imports before unparking.')\"",
    "generate": "node dist/generate-matrix-cli.js",
    "check:matrix": "node dist/generate-matrix-cli.js --check",
    "benchmark:report": "node dist/benchmark/generate-report-cli.js",
    "benchmark:check": "node dist/benchmark/generate-report-cli.js --check",
    "benchmark:run": "node dist/benchmark/arm-runner-cli.js"
  },
  "dependencies": {
    "crumbtrail-core": "workspace:^",
    "crumbtrail-node": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsup": "^8.4.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.10"
  }
}
